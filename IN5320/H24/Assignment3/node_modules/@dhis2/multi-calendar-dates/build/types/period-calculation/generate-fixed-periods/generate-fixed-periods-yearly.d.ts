import { Temporal } from '@js-temporal/polyfill';
import { SupportedCalendar } from '../../types';
import { FixedPeriod, PeriodType } from '../types';
declare type GenerateFixedPeriodsYearly = (options: {
    year: number;
    periodType: PeriodType;
    calendar: SupportedCalendar;
    endsBefore?: Temporal.PlainDate;
    yearsCount: number | null;
    locale: string;
}) => Array<FixedPeriod>;
declare const generateFixedPeriodsYearly: GenerateFixedPeriodsYearly;
export default generateFixedPeriodsYearly;
