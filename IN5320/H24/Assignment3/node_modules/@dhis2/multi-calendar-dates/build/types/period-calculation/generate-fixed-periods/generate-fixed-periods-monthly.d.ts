import { Temporal } from '@js-temporal/polyfill';
import { SupportedCalendar } from '../../types';
import { FixedPeriod, PeriodType } from '../types';
declare type GenerateFixedPeriodsMonthly = (options: {
    year: number;
    periodType: PeriodType;
    calendar: SupportedCalendar;
    locale: string;
    endsBefore?: Temporal.PlainDate;
}) => Array<FixedPeriod>;
declare const generateFixedPeriodsMonthly: GenerateFixedPeriodsMonthly;
export default generateFixedPeriodsMonthly;
