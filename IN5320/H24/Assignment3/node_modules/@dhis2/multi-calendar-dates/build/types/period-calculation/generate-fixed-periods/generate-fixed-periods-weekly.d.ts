import { Temporal } from '@js-temporal/polyfill';
import { SupportedCalendar } from '../../types';
import { FixedPeriod, PeriodType } from '../types';
declare type GenerateFixedPeriodsWeekly = (options: {
    year: number;
    periodType: PeriodType;
    calendar: SupportedCalendar;
    startingDay: number; /** 1 is Monday */
    endsBefore?: Temporal.PlainDate;
}) => Array<FixedPeriod>;
declare const generateFixedPeriodsWeekly: GenerateFixedPeriodsWeekly;
export default generateFixedPeriodsWeekly;
