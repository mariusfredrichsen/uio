"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;
var _index = require("../../utils/index");
var _index2 = require("../daily-periods/index");
const getAdjacentDailyFixedPeriods = _ref => {
  let {
    period,
    steps,
    calendar,
    locale
  } = _ref;
  const startDate = (0, _index.fromAnyDate)({
    date: period.startDate,
    calendar
  });
  const adjacentPeriods = [];
  const iterations = Math.abs(steps);
  for (let i = 0; i < iterations; ++i) {
    const days = i + 1;
    const nextDay = steps > 0 ? startDate.add({
      days
    }) : startDate.subtract({
      days
    });
    const nextPeriod = (0, _index2.buildDailyFixedPeriod)({
      date: nextDay,
      calendar,
      locale
    });
    steps > 0 ? adjacentPeriods.push(nextPeriod) : adjacentPeriods.unshift(nextPeriod);
  }
  return adjacentPeriods;
};
var _default = getAdjacentDailyFixedPeriods;
exports.default = _default;