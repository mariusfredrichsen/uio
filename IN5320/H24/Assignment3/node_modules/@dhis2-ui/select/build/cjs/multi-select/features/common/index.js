"use strict";

var _cypressCucumberPreprocessor = require("@badeball/cypress-cucumber-preprocessor");
(0, _cypressCucumberPreprocessor.Given)('a MultiSelect with options and onChange handler is rendered', () => {
  cy.visitStory('MultiSelect', 'With options and on change');
});
(0, _cypressCucumberPreprocessor.Given)('a MultiSelect with options, a selection and onChange handler is rendered', () => {
  cy.visitStory('MultiSelect', 'With options, a selection and on change');
});
(0, _cypressCucumberPreprocessor.Given)('a MultiSelect is rendered to which options can be added', () => {
  cy.visitStory('MultiSelect', 'With options that can be added to the input');
  cy.get('[data-test="dhis2-uicore-multiselect"]').should('exist');
});
(0, _cypressCucumberPreprocessor.Given)('the MultiSelect is open', () => {
  cy.get('[data-test="dhis2-uicore-select-input"]').click();
  cy.contains('option one').should('be.visible');
  cy.contains('option two').should('be.visible');
  cy.contains('option three').should('be.visible');
});
(0, _cypressCucumberPreprocessor.When)('the MultiSelect input is clicked', () => {
  cy.get('[data-test="dhis2-uicore-select-input"]').click();
});
(0, _cypressCucumberPreprocessor.When)('the user clicks the backdrop layer', () => {
  cy.get('[data-test="dhis2-uicore-layer"]').click();
});
(0, _cypressCucumberPreprocessor.Then)('the options are not rendered', () => {
  cy.contains('option one').should('not.exist');
  cy.contains('option two').should('not.exist');
  cy.contains('option three').should('not.exist');
});
(0, _cypressCucumberPreprocessor.Then)('the options are displayed', () => {
  cy.contains('option one').should('be.visible');
  cy.contains('option two').should('be.visible');
  cy.contains('option three').should('be.visible');
});
(0, _cypressCucumberPreprocessor.Then)('the MultiSelect has focus', () => {
  cy.focused().should('have.attr', 'data-test', 'dhis2-uicore-select-input');
});