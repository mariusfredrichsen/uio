"use strict";

var _enzyme = require("enzyme");
var _react = _interopRequireDefault(require("react"));
var _imageAvatar = require("./image-avatar.js");
var _textAvatar = require("./text-avatar.js");
var _userAvatar = require("./user-avatar.js");
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
describe('UserAvatar', () => {
  it('renders an ImageAvatar when a avatarId is provided', () => {
    const wrapper = (0, _enzyme.shallow)( /*#__PURE__*/_react.default.createElement(_userAvatar.UserAvatar, {
      name: "John Doe",
      avatarId: "123"
    }));
    expect(wrapper.find(_imageAvatar.ImageAvatar)).toHaveLength(1);
  });
  it('renders a TextAvatar when no avatarId is provided', () => {
    const wrapper = (0, _enzyme.shallow)( /*#__PURE__*/_react.default.createElement(_userAvatar.UserAvatar, {
      name: "John Doe"
    }));
    expect(wrapper.find(_textAvatar.TextAvatar)).toHaveLength(1);
  });
  describe('sizes', () => {
    const sizes = ['extrasmall', 'small', 'medium', 'large', 'extralarge'];
    for (const size of sizes) {
      it(`should accept size prop "${size}"`, () => {
        const props = {
          name: 'John Doe',
          [size]: true
        };
        const wrapper = (0, _enzyme.shallow)( /*#__PURE__*/_react.default.createElement(_userAvatar.UserAvatar, props));
        expect(wrapper.hasClass(size)).toBe(true);
      });
    }
  });
});