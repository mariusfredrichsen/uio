"use strict";

var _cypressCucumberPreprocessor = require("@badeball/cypress-cucumber-preprocessor");
(0, _cypressCucumberPreprocessor.Given)('the selector bar item is closed', () => {
  cy.visitStory('SelectorBarItem', 'closed no value');
});
(0, _cypressCucumberPreprocessor.Given)('the selector bar item is open', () => {
  cy.visitStory('SelectorBarItem', 'open no value');
});
(0, _cypressCucumberPreprocessor.When)('the user opens the selector bar item', () => {
  cy.contains('Selection bar item', {
    selector: 'button'
  }).click();
});
(0, _cypressCucumberPreprocessor.When)('the user closes the selector bar item', () => {
  cy.get('body').click('topLeft');
});
(0, _cypressCucumberPreprocessor.Then)('the children should be displayed', () => {
  cy.get('[data-test="dhis2-uiwidgets-singleselectfield"]').should('exist');
});
(0, _cypressCucumberPreprocessor.Then)('the children should not be displayed', () => {
  cy.get('[data-test="dhis2-uiwidgets-singleselectfield"]').should('not.exist');
});