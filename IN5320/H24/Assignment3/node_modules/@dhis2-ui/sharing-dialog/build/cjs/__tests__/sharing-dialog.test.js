"use strict";

var _appRuntime = require("@dhis2/app-runtime");
var _enzyme = require("enzyme");
var _react = _interopRequireDefault(require("react"));
var _sharingDialog = require("../sharing-dialog.js");
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
describe('<SharingDialog />', () => {
  it('calls the onClose callback when the close button is clicked', () => {
    const requiredProps = {
      id: 'sharing-test',
      type: 'visualization',
      onClose: () => {}
    };
    const spy = jest.fn();
    const wrapper = (0, _enzyme.mount)( /*#__PURE__*/_react.default.createElement(_sharingDialog.SharingDialog, _extends({}, requiredProps, {
      onClose: spy
    })), {
      wrappingComponent: _appRuntime.CustomDataProvider
    });
    const elements = wrapper.find('button').filterWhere(el => el.text().includes('Close'));
    expect(elements).toHaveLength(1);
    expect(spy).toHaveBeenCalledTimes(0);
    elements.first().simulate('click');
    expect(spy).toHaveBeenCalledTimes(1);
  });
});