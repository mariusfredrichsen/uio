"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getModeByModifierKey = void 0;
var _modes = require("./modes.js");
/**
 * @param {Object} args
 * @param {bool} args.altKey
 * @param {bool} args.shiftKey
 * @param {bool} args.ctrlKey
 * @param {bool} args.metaKey
 * @return {string}
 */
const getModeByModifierKey = _ref => {
  let {
    altKey,
    shiftKey,
    ctrlKey,
    metaKey
  } = _ref;
  const keys = [altKey, shiftKey, ctrlKey, metaKey];
  const amountKeyPressed = keys.filter(v => v);
  const moreThanOneKeyPressed = amountKeyPressed.length;
  if (moreThanOneKeyPressed !== 1) {
    return _modes.REPLACE_MODE;
  }
  if (altKey || ctrlKey || metaKey) {
    return _modes.ADD_MODE;
  }
  if (shiftKey) {
    return _modes.RANGE_MODE;
  }

  // default to replace mode
  return _modes.REPLACE_MODE;
};
exports.getModeByModifierKey = getModeByModifierKey;