import { Temporal } from '@js-temporal/polyfill';
import { calendars } from './constants/calendars';
import { numberingSystems } from './constants/numberingSystems';
export declare type SupportedCalendar = typeof calendars[number];
export declare type SupportedNumberingSystem = typeof numberingSystems[number];
export declare type WeekDayFormat = 'narrow' | 'short' | 'long';
export declare type PickerOptions = Partial<ResolvedLocaleOptions>;
export declare type ResolvedLocaleOptions = {
    calendar: SupportedCalendar;
    locale: string;
    timeZone: Temporal.TimeZoneLike;
    numberingSystem: string;
    weekDayFormat: WeekDayFormat;
};
