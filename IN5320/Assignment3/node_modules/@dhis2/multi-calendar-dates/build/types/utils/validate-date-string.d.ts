import type { SupportedCalendar } from '../types';
export declare function validateDateString(dateString: string, { calendar, minDateString, maxDateString, strictValidation, format, }?: {
    calendar?: SupportedCalendar;
    minDateString?: string;
    maxDateString?: string;
    strictValidation?: boolean;
    format?: 'YYYY-MM-DD' | 'DD-MM-YYYY';
}): {
    isValid: boolean;
    errorMessage?: string;
    warningMessage?: string;
    year?: number;
    month?: number;
    day?: number;
};
