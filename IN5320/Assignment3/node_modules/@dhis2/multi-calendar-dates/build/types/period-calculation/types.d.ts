import { periodTypes } from './period-types';
export declare type PeriodType = typeof periodTypes[number];
export declare type FixedPeriod = {
    periodType: PeriodType;
    id: string;
    iso?: string;
    name: string;
    displayName: string;
    startDate: string;
    endDate: string;
};
