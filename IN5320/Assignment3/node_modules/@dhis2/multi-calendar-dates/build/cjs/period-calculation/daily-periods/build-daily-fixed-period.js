"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;
var _index = require("../../utils/index");
const {
  localiseDateLabel
} = _index.localisationHelpers;
const buildDailyFixedPeriod = _ref => {
  let {
    date,
    calendar,
    locale
  } = _ref;
  const year = date.year;
  const nextDayMonthLabel = String(date.month).padStart(2, '0');
  const nextDayLabel = String(date.day).padStart(2, '0');
  const value = `${year}${nextDayMonthLabel}${nextDayLabel}`;
  const displayName = localiseDateLabel(date, {
    calendar,
    locale
  }, {
    dateStyle: 'long'
  });
  return {
    periodType: 'DAILY',
    id: value,
    iso: value,
    displayName,
    name: (0, _index.formatDate)(date),
    startDate: (0, _index.formatDate)(date),
    endDate: (0, _index.formatDate)(date)
  };
};
var _default = buildDailyFixedPeriod;
exports.default = _default;