"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;
var _polyfill = require("@js-temporal/polyfill");
var _utils = require("../../utils");
const doesPeriodEndBefore = _ref => {
  let {
    period,
    date,
    calendar
  } = _ref;
  const periodStartDay = (0, _utils.fromAnyDate)({
    calendar,
    date: period.startDate
  });
  const periodEndDay = (0, _utils.fromAnyDate)({
    calendar,
    date: period.endDate
  });
  const periodStartsOnOrAfterDate = _polyfill.Temporal.PlainDate.compare(date, periodStartDay) < 1;
  const endsBeforeAfterPeriodStart = _polyfill.Temporal.PlainDate.compare(periodStartDay, date) === -1;
  const periodEndsOnOrBeforeDate = _polyfill.Temporal.PlainDate.compare(date, periodEndDay) < 1;
  return periodStartsOnOrAfterDate || endsBeforeAfterPeriodStart && periodEndsOnOrBeforeDate;
};
var _default = doesPeriodEndBefore;
exports.default = _default;