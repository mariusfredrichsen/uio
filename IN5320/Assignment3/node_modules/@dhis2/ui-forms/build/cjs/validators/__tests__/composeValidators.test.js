"use strict";

var _composeValidators = require("../composeValidators.js");
var _email = require("../email.js");
var _hasValue = require("../hasValue.js");
describe('composeValidators', () => {
  const validator = (0, _composeValidators.composeValidators)(_hasValue.hasValue, _email.email);
  it('should return undefined for valid values', () => {
    expect(validator('test@dhis2.org')).toBe(undefined);
  });
  it('should return the required message for empty values', () => {
    const validator = (0, _composeValidators.composeValidators)(_hasValue.hasValue, _email.email);
    expect(validator('')).toBe(_hasValue.hasValueMessage);
  });
  it('should return invalid e-mail message for malformed strings', () => {
    const validator = (0, _composeValidators.composeValidators)(_hasValue.hasValue, _email.email);
    expect(validator('test@dhis2.')).toBe(_email.invalidEmailMessage);
  });
});