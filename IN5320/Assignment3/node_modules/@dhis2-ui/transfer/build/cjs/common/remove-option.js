"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.removeOption = void 0;
var _findOptionIndex = require("./find-option-index.js");
/**
 * @param {Object[]} options
 * @param {Object} option
 * @returns {Object}
 */
const removeOption = (options, option) => {
  const index = (0, _findOptionIndex.findOptionIndex)(options, option);
  if (index === -1) {
    return options;
  }
  if (index === 0) {
    return options.slice(1);
  }
  return [...options.slice(0, index), ...options.slice(index + 1)];
};
exports.removeOption = removeOption;