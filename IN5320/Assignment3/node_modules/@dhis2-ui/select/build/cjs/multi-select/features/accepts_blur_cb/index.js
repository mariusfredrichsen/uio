"use strict";

var _cypressCucumberPreprocessor = require("@badeball/cypress-cucumber-preprocessor");
(0, _cypressCucumberPreprocessor.Given)('a MultiSelect with onBlur handler is rendered', () => {
  cy.visitStory('MultiSelect', 'With on blur');
});
(0, _cypressCucumberPreprocessor.Then)('the onBlur handler is called', () => {
  cy.window().should(win => {
    expect(win.onBlur).to.be.calledOnce;
    expect(win.onBlur).to.be.calledWith({
      selected: []
    });
  });
});